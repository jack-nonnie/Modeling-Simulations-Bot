"use strict";

const localtunnel = require("localtunnel");
const promptly = require("promptly");

class Dev {
    static dev(port, subdomain) {
        const options = {
            subdomain: subdomain,
        };
    }
}

module.exports = Dev;
